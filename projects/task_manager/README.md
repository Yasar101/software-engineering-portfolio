# Task Manager Application

An SQLite-backed repository with validated task creation, explicit completion transitions, and status filtering. Use `sqlite3.connect("tasks.db")` for persistence or `:memory:` for tests.

