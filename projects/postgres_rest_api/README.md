# PostgreSQL REST API

A framework-neutral REST service layer with validation, status-code semantics, a repository port, an in-memory test adapter, and production PostgreSQL schema. A deployment adapter can implement `ItemRepository` using psycopg without coupling SQL to HTTP handlers.

