# AI Developer Assistant

A provider-neutral retrieval pipeline that ranks repository context, constructs bounded prompts, and rejects likely credentials. Inject any callable model provider; unit tests use deterministic fakes.

