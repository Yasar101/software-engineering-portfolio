# Distributed AI Systems Platform

A concurrency-safe reference job scheduler for AI workloads. It supports submission, worker claims, expiring leases, ownership checks, bounded retries, and terminal states. The in-memory store can be replaced with a transactional database or queue.

