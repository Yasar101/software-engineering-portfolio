# Python CLI Calculator

A safe command-line calculator using `Decimal` rather than dynamic evaluation.

```bash
python3 -m projects.cli_calculator.calculator "12.5 * 4"
```

Supports addition, subtraction, multiplication, and division with useful errors.

